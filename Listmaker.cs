interface Listmaker
{
    // Create interface that can create:
    // list of strings for one student's order and
    // list of all the student objects for totaler to iterate through.

    public void MakeList();
}
